# Cast for Soultale.
#
# This file holds every speaker code that the prologue and Volume 1 use. The
# prologue is chapters 0 to 2 and Volume 1 is chapters 3 to 22. Together they
# have 44 codes and 687 dialogue lines.
#
# The build stops when a chapter uses a code that this file does not have. The
# error names the code and the chapter. Add the code here, then build again.
#
# Give each entry a name and a voice. The name is for you and for the reports.
# The renderer uses the voice.
#
# What a voice is depends on the engine that speaks it:
#
#   chatterbox   A path to a recording of the character, read from this
#                directory: "voices/blook.wav". Ten to twenty seconds of clean
#                speech is enough, and what is in the recording is what comes
#                out, including the room it was recorded in.
#   chatterbox-turbo
#                The same paths, through a newer model that reads several
#                times faster, holds one loudness by itself, and takes five
#                seconds of reference instead of twenty.
#   kokoro       One of its own names, such as "af_heart". The first letter
#                says the accent: a for American, b for British.
#   espeak       A language and a variant: "en-us", "en-gb+Alicia".
#
# 'openbook check --engine chatterbox' says which recordings are not there
# yet, so a missing one is found before a render and not after it.
#
# HOW MUCH FEELING
#
# An engine that reads with feeling takes an exaggeration. What it means
# depends on the model, because they do not agree on where flat is.
#
#   chatterbox        0.3 narration, 0.7 dialogue.
#   chatterbox-turbo  0.5 for both, and it does nothing to the sound.
#
# The older pair says that a narrator holds one level for hours while a
# character in a fantasy is frightened, or lying, or giving an order.
#
# Turbo ignores the number. One line read at 0.0, 0.5 and 1.0 from one seed
# comes back bit for bit the same, so the setting cannot make this model read
# with more feeling. It is still part of the key of a line, so changing it
# gives a different reading of that line, and the two kinds share one number
# because this model draws no line between them.
#
# A feeling on Turbo comes from a tag in corrections.toml, [happy] or [fear]
# and the rest. That is the only thing it listens to.
#
# DO NOT WRITE 'exaggeration' FOR A NEW CHARACTER while Turbo is the engine.
#
# It cannot do the thing its name promises. What it still does is change the
# key of every line that character says, so writing one makes that character
# again and gives you a different reading, which is not what somebody adding
# a voice is asking for. Give the character a recording and stop there.
#
#   [cast.BLK]
#   voice = "voices/blook.wav"
#
# One entry below has a number, and it stays only because the lines it made
# are made and were listened to. Removing it now would remake them for no
# gain. Read that entry as history rather than as an example.
#
# On the older chatterbox engine the number means what it says. Leave it out
# and the kind of the line decides. It is part of the key of each line, so
# changing it for one character remakes that character and nobody else, and
# changing the number for dialogue leaves the narration alone.


[narrator]
# The narrator speaks 79 percent of the book. Choose this voice first, and
# listen to it for several minutes before you accept it. This is also the one
# voice where a wandering model costs most, because it reads a third of a
# million words and a dropped one is found only by listening.
#
# A recording, so this needs '--engine chatterbox-turbo'. Everything the
# recording carries comes out in the reading: the accent, the pace, and the
# room it was made in.
voice = "voices/narrator.wav"


# A narrator for a run of chapters, where the telling changes hands.
#
# Soultale does not keep one narrator. The prologue is told from outside: no
# first person in all 5,306 words of its narration. Chapter 3 opens in the
# first person and never goes back, and that voice is Blook, so the narrator
# of Volume 1 and the voice of BLK are one person and take one recording.
# Later volumes change again, chapter by chapter, to whoever the point of
# view belongs to.
#
# A range wins over [narrator] above, which answers for every chapter no
# range covers. Two ranges over one chapter are refused.
#
#   [[narrator_range]]
#   chapters = "3-22"
#   name     = "Blook"
#   voice    = "voices/blook.wav"
#
# TEMPORARY. Blook tells Volume 1, so the narrator of chapters 3 to 22 and
# the code BLK are one person and take one voice. This is a Kokoro name and
# not a recording, so that the volume can be heard before 37 recordings
# exist.

[[narrator_range]]
chapters = "3-22"
name     = "Blook"
voice    = "am_michael"


[host]
# The voice that speaks to the listener rather than inside the book. It reads
# the intro and the outro, where the video says hello and asks for a
# subscription.
#
# It is not a character and it is not in the cast, because nobody in the book
# ever hears it. Leave this out and the narrator reads those words, which is
# what happened before this entry existed.
#
# TEMPORARY, with the rest of the Kokoro cast below. The real voice is
# "voices/host.wav", and it has to go back before the prologue is rendered
# again: Turbo cannot speak a Kokoro name and will say so. The audio the
# prologue already has is in the cache and is not touched by this.
voice = "af_sky"


# LEADS
#
# Two codes carry most of the dialogue of the volume. Give each one a voice
# that no other character uses.

# 160 lines. First in chapter 4.
[cast.BLK]
name  = "Blook"
voice = "am_michael"

# 54 lines. First in chapter 0.
[cast.INK]
name         = ""
voice        = "voices/ink.wav"
# Written when this number was thought to set how much feeling a character
# reads with. It does not, on this engine. It is left alone because these 54
# lines are made and have been heard, and removing it would change the key and
# make them all again. Do not copy it into a new entry.
exaggeration = 0.4


# RECURRING
#
# Six of these speak in the prologue and all six now hold a recording. The
# rest are for Volume 1 and still wait for a voice, so a render of the
# prologue works while Volume 1 does not.
#
# These codes have 5 to 39 lines each. Give each one a voice. Two of them can
# share a voice when they never appear in the same chapter, though a listener
# who meets both is owed two voices, so share only where you have to.
#
# What you run out of has changed. A fixed list of voices runs out at its last
# name. A recording never does, and every voice costs a recording instead.

# 39 lines. First in chapter 18.
[cast.LCH]
name  = "Louch"
voice = "am_fenrir"

# 35 lines. First in chapter 0.
[cast.ZER]
name  = ""
voice = "voices/zero.wav"

# 36 lines. First in chapter 4.
[cast.GBR]
name  = "Gabriel"
voice = "am_adam"

# 32 lines. First in chapter 13.
[cast.RGN]
name  = "Rogen"
voice = "am_onyx"

# 29 lines. First in chapter 17.
[cast.RMI]
name  = "Remi"
voice = "bm_george"

# 26 lines. First in chapter 12.
[cast.VLT]
name  = "Velvet"
voice = "af_heart"

# 23 lines. First in chapter 4.
[cast.LCF]
name  = "Lucifer"
voice = "am_eric"

# 22 lines. First in chapter 15.
[cast.SMP]
name  = "Sampo"
voice = "bm_lewis"

# 21 lines. First in chapter 11.
[cast.ZPH]
name  = "Zephine"
voice = "af_bella"

# 21 lines. First in chapter 16.
[cast.MRL]
name  = "Mirelle"
voice = "af_nicole"

# 20 lines. First in chapter 10.
[cast.EMR]
name  = "Emory"
voice = "am_liam"

# 20 lines. First in chapter 16.
[cast.DRK]
name  = "Draken"
voice = "bm_daniel"

# 19 lines. First in chapter 9.
[cast.MRW]
name  = "Marlow"
voice = "am_echo"

# 18 lines. First in chapter 14.
[cast.CAL]
name  = "Calista"
voice = "bf_emma"

# 15 lines. First in chapter 8.
[cast.SNA]
name  = "Sienna"
voice = "af_sarah"

# 13 lines. First in chapter 1.
[cast.DRM]
name  = ""
voice = "voices/dream.wav"

# 12 lines. First in chapter 1.
[cast.END]
name  = ""
voice = "voices/theend.wav"

# 9 lines. First in chapter 4.
[cast.VTH]
name  = "Vazroth"
voice = "bm_fable"

# 7 lines. First in chapter 1.
[cast.BLB]
name  = ""
voice = "voices/blueberry.wav"

# 6 lines. First in chapter 14.
[cast.MAN]
name  = "Man"
voice = "am_puck"

# 5 lines. First in chapter 7.
[cast.AQL]
name  = "Aquila"
voice = "af_kore"

# 5 lines. First in chapter 17.
[cast.MRC]
name  = "Marcus"
voice = "am_santa"


# SMALL PARTS
#
# These codes have 4 lines or fewer. Several of them can share one voice,
# because a listener does not hold a voice with one line in memory. Give the
# same voice to more than one code here when the codes are far apart. This is
# where sharing costs least, and it is the first place to spend one recording
# on several characters.

# 4 lines. First in chapter 1.
[cast.EDG]
name  = ""
voice = "voices/edge.wav"

# 4 lines. First in chapter 9.
[cast.VLG]
name  = "Villager"
voice = "am_fenrir"

# 3 lines. First in chapter 0. The nameless voice of the prologue.
#
# This is Zero. It takes his voice and keeps a name of its own, so that a
# listener hears who is speaking while a caption does not say so. Change this
# voice whenever ZER changes, or the two come apart.
[cast."///"]
name  = ""
voice = "voices/zero.wav"

# 3 lines. First in chapter 12.
[cast.VNT]
name  = "Vincent"
voice = "am_adam"

# 3 lines. First in chapter 12.
[cast.SRH]
name  = "Sarah"
voice = "bf_alice"

# 3 lines. First in chapter 13.
[cast.AST]
name  = "Astra"
voice = "af_nova"

# 3 lines. First in chapter 15.
[cast.CHD]
name  = "Child"
voice = "af_aoede"

# 3 lines. First in chapter 17.
[cast.KLN]
name  = "Kellan"
voice = "am_onyx"

# 2 lines. First in chapter 9.
[cast.STW]
name  = "Steward"
voice = "bm_george"

# 2 lines. First in chapter 12.
[cast.ST1]
name  = "Stranger 1"
voice = "bf_lily"

# 1 line. First in chapter 5.
[cast."KID #1"]
name  = "Kid 1"
voice = "bm_lewis"

# 1 line. First in chapter 5.
[cast."KID #2"]
name  = "Kid 2"
voice = "am_liam"

# 1 line. First in chapter 7.
[cast.MAR]
name  = "Marchant"
voice = "am_eric"

# 1 line. First in chapter 10.
[cast.BRN]
name  = "Brennan"
voice = "bm_daniel"

# 1 line. First in chapter 10.
[cast.MIN]
name  = "Miner"
voice = "am_echo"

# 1 line. First in chapter 12.
[cast.ST2]
name  = "Stranger 2"
voice = "bm_fable"

# 1 line. First in chapter 15.
[cast.BCK]
name  = "Baker"
voice = "am_santa"

# 1 line. First in chapter 15.
[cast.WMN]
name  = "Woman"
voice = "af_river"

# 1 line. First in chapter 16.
[cast.MCH]
name  = "Mechanic"
voice = "am_fenrir"

# 1 line. First in chapter 22.
[cast.MOM]
name  = "Mom"
voice = "af_jessica"


# UNKNOWN SPEAKERS
#
# The code ??? is not one character. It is a different character in each part
# of the book, so it cannot have one entry.
#
# Give it one entry for each group of chapters. The renderer finds the entry
# from the chapter number. A ??? line in a chapter that no entry covers stops
# the build, which is correct: a new unknown character needs a new voice.
#
# Volume 1 has no ??? line, so these entries are for later volumes:
#
#   [[cast_range."???"]]
#   chapters = "38"
#   name     = ""
#   voice    = ""
#
#   [[cast_range."???"]]
#   chapters = "115-120"      # 51 lines, the largest group
#   name     = ""
#   voice    = ""
#
# The other groups are chapter 73, chapter 128, and chapter 175.
#
# A group can use the voice of a character that the story names later. The
# listener then hears the connection when they read the book again. Or the
# group can keep a voice of its own. Choose this for each group.
